<form method="post" action="{{ path('piste_delete', {'id': piste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piste.id) }}">
    <button class="btn">Delete</button>
</form>
