{% extends 'base.html.twig' %}

{% block title %}New Notification{% endblock %}

{% block body %}
    <h1>Create new Notification</h1>

    {{ form_start(form) }}
    {{ form_row(form.emailtoreply , {'label':'Email','attr': {'placeholder':'Votre email ...'}}) }}
    <button type="submit" class="btn btn-primary"> Send</button>
    {{  form_end(form) }}
    <a href="{{ path('notification_index') }}">back to list</a>
{% endblock %}
