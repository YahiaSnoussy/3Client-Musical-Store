<form method="post" action="{{ path('carteb_delete', {'id': carteb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carteb.id) }}">
    <button class="btn">Delete</button>
</form>
