{% extends '1base.html.twig' %}

{% block title %}Livreur index{% endblock %}

{% block body %}
    <h1>Livreur index</h1>

    <table class="table">
        <thead>
            <tr>

                <th>Nom</th>
                <th>Prenom</th>
                <th>Img</th>
                <th>adress</th>
            </tr>
        </thead>
        <tbody>
        {% for livreur in livreurs %}
            <tr>

                <td>{{ livreur.nom }}</td>
                <td>{{ livreur.prenom }}</td>
                <td><img src={{ livreur.img }}/></td>
                <td>
                    <a href="{{ path('livreur_show', {'id': livreur.id}) }}">show</a>

                </td>
                <td>


                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
